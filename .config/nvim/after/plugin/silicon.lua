-- local silicon = require("silicon")

-- vim.api.nvim_create_user_command("Silicon", function(opts)
-- 	silicon.setup({
-- 		output = opts.args,
-- 	})

-- 	silicon.visualise_cmdline({})
-- end, { nargs = 1, complete = "dir", range = "%" })
